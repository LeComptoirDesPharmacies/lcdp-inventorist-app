on:
  push:
    branches:
      - 'develop'
      - 'bugfix/**'

name: Snapshot App

jobs:
  Package_app:
    name: Create Snapshot
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-15-intel, macos-15]
        python-version: ['3.12']
        include:
          - os: macos-15-intel
            label: macos-intel
            path: ~/Library/Caches/pip
          - os: macos-15
            label: macos-applesilicon
            path: ~/Library/Caches/pip
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      - uses: actions/cache@v4
        with:
          path: ${{ matrix.path }}
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: |
          pip install -r requirements.txt --extra-index-url https://${{ secrets.DEPLOY_GEMFURY_TOKEN }}:@pypi.fury.io/lcdp/
      - uses: actions/setup-java@v4
        with:
          distribution: 'adopt'
          java-version: '11'
      - name: Prepare app
        run: python prepare.py
        env:
          LCDP_ENVIRONMENT: staging
          PROVIDER_HOST: www.lcdp.ovh
          IS_PROVIDER_SECURE: "True"
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
      - run: pip install pyinstaller
      - name: Compute safe branch name
        id: branch
        run: echo "safe=$(echo '${{ github.ref_name }}' | tr '/' '-')" >> "$GITHUB_OUTPUT"
      - name: Package project
        run: pyinstaller app.spec --distpath ./${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app
      - name: Add macOS install script
        run: |
          cat > "./${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app/install.command" <<'EOF'
          #!/bin/bash
          set -e
          cd "$(dirname "$0")"
          APP="LCDP - Inventorist App.app"
          echo "Removing quarantine attribute..."
          xattr -cr "$APP"
          echo "Re-signing ad-hoc locally (required on Apple Silicon)..."
          codesign --force --deep --sign - "$APP"
          echo "Launching $APP..."
          open "$APP"
          EOF
          chmod +x "./${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app/install.command"
      - name: Zip project
        run: 7z a ${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app.zip ./${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app/*
      - name: Upload Release
        uses: ncipollo/release-action@v1
        with:
          name: snapshot-${{ github.run_id }}
          tag: ${{ github.run_id }}-snapshot
          prerelease: true
          commit: ${{ github.sha }}
          artifacts: ${{ matrix.label }}-${{ steps.branch.outputs.safe }}-app.zip
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true